# Public site URL for canonical links and Open Graph image URLs (no trailing slash)
# VITE_SITE_URL=https://www.example.com

# POST quote JSON to your backend (see src/services/submitQuoteRequest.js). Leave unset for demo mode.
# VITE_QUOTE_API_URL=https://api.example.com/v1/quotes
